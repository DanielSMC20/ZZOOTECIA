# Copia este archivo a `.env` y adapta los valores para tu entorno local

# Backend (mock o servicio que el chatbot consulta)
BACKEND_URL=http://localhost:8080

# MySQL
DB_USER=root
DB_PASSWORD=DanielSMC123-
DB_HOST=localhost
DB_NAME=zzootec_db

# Opcional: URL completa de conexión (si prefieres usarla en lugar de las piezas anteriores)
# DATABASE_URL=mysql+mysqlconnector://user:pass@host/dbname

# Pool de DB (opcional)
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
